# Copilot Instructions

Use a currently available Copilot model such as GPT-5.4 mini for cloud-agent work in this repository.
Do not rely on gpt-5.3-codex, which is unavailable in the current runtime.

Keep changes minimal and focused.
Validate backend changes with `pytest` and frontend changes with `cd frontend && npm run build` before pushing.