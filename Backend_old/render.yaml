services:
  - type: web
    name: ai-job-screening-api
    env: python
    buildCommand: ""
    # Increase timeout to 120s to prevent worker kill on slow LLM calls
    startCommand: gunicorn --timeout 120 app:app
    plan: free
